# ============================================================================
# vortex_backtest 配置模板（密钥/参数层）。
#
# 配置分层：common 生成的 env（vortex_common/config/vortex.generated.env，定义
# 端口/绑定地址/TZ/workspace 根等基础设施量）+ 本文件（仅本仓密钥）。
# 端口/HOST/TZ/挂载路径不在此处——它们由 `vortex run up backtest` 从 common 注入。
#
# 使用方式：cp .env.example .env，然后在 .env 里填入真实密钥。
# .env 已在 .gitignore 中，不会被提交（本 .env.example 模板才入库）。
# ============================================================================

# 写接口 token（建账户/下单/提交回测）—— 密钥，留空待填（不入库、不上页面）。
# token 守卫为 design/03 阶段6 待实现；在此之前靠默认只绑回环保证安全。
# 对外暴露（common 把 VORTEX_BACKTEST_BIND_ADDR 设成 0.0.0.0）前【必填】此项。
# 生成一个：python3 -c "import secrets; print(secrets.token_hex(24))"
VORTEX_BACKTEST_TOKEN=
